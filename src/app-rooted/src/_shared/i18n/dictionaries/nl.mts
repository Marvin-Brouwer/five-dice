import { dictionary, translation } from '@rooted/localization'

export const NewLine = '\n'

export default dictionary(
	translation('Settings', 'Instellingen'),
	translation(`Enter${NewLine}score`, `Voer${NewLine}score in`),
)
