---
import "./die.css";
import "./text-die.css";

interface Props {
    value: string
}

const { value } = Astro.props;
---

<span class="die" aria-valuetext={value}>
    <span class="text">{value}</span>
</span>